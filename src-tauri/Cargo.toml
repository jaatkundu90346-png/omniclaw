[package]
name = "omniclaw-desktop"
version = "0.1.0"
description = "OmniClaw desktop shell"
authors = ["OmniClaw"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
